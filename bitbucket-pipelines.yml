image: alpine:latest
pipelines:
  default:
    - step:
        script:
          - docker login -u olprog -p d4c86d0c-d65f-4b55-81f4-5d4bab758c69
          - docker build -t olprog/media-organizer:latest .
          - docker push olprog/media-organizer:latest

options:
  docker: true
